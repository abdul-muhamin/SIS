"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.downvoteConversationMessage = exports.upvoteConversationMessage = exports.deleteConversationMessage = exports.updateConversationMessage = exports.createConversationMessage = exports.fetchConversationMessage = exports.listConversationMessages = void 0;
function listConversationMessages(client, conversationId, request) {
    let url = `/api/v1/conversation/${conversationId}/message/list`;
    const response = client.clientFetch(url, { query: request });
    return response;
}
exports.listConversationMessages = listConversationMessages;
async function fetchConversationMessage(client, conversationId, messageId) {
    const url = `/api/v1/conversation/${conversationId}/message/${messageId}/fetch`;
    const response = await client.clientFetch(url);
    return response;
}
exports.fetchConversationMessage = fetchConversationMessage;
async function createConversationMessage(client, conversationId, request) {
    const url = `/api/v1/conversation/${conversationId}/message/create`;
    const response = await client.clientFetch(url, {
        record: {
            ...request,
        },
    });
    return response;
}
exports.createConversationMessage = createConversationMessage;
async function updateConversationMessage(client, conversationId, messageId, request) {
    const url = `/api/v1/conversation/${conversationId}/message/${messageId}/update`;
    const response = await client.clientFetch(url, {
        record: {
            ...request,
        },
    });
    return response;
}
exports.updateConversationMessage = updateConversationMessage;
async function deleteConversationMessage(client, conversationId, messageId) {
    const url = `/api/v1/conversation/${conversationId}/message/${messageId}/delete`;
    const response = await client.clientFetch(url, {
        record: {},
    });
    return response;
}
exports.deleteConversationMessage = deleteConversationMessage;
async function upvoteConversationMessage(client, conversationId, messageId, request) {
    const url = `/api/v1/conversation/${conversationId}/message/${messageId}/upvote`;
    const response = await client.clientFetch(url, {
        record: {
            value: 100,
            ...request,
        },
    });
    return response;
}
exports.upvoteConversationMessage = upvoteConversationMessage;
async function downvoteConversationMessage(client, conversationId, messageId, request) {
    const url = `/api/v1/conversation/${conversationId}/message/${messageId}/downvote`;
    const response = await client.clientFetch(url, {
        record: {
            value: -100,
            ...request,
        },
    });
    return response;
}
exports.downvoteConversationMessage = downvoteConversationMessage;
